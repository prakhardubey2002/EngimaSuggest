# Monorepo root — Next (`apps/web` envDir) and Nest (`apps/api` loads ../.env) read this file.

BACKENED_PORT=3001
BACKENED_URL=http://127.0.0.1:3001

# Optional: Next API route logging (NDJSON → apps/web/logger.json)
# ENVIRONMENT=development
