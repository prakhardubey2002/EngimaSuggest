# Nest API base URL (no trailing slash). Used by POST /api/parse → {BACKENED_URL}/parse
BACKENED_URL=http://127.0.0.1:3001

# When ENVIRONMENT=development, API logs append NDJSON lines to logger.json (cwd = apps/web)
ENVIRONMENT=development
